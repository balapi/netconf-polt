bcm_make_normal_option(MFC_RELAY BOOL "Build MFC Relay support" n)
if(MFC_RELAY)
    bcm_module_name(mfc_relay)
    bcm_module_dependencies(PRIVATE os api netconf_modules mfc_relay_grpc)
    bcm_module_header_paths(PUBLIC .)
    bcm_module_definitions(PUBLIC -DMFC_RELAY)
    bcm_module_srcs(mfc_relay_common.cc mfc_relay_client.cc)
    bcm_create_lib_target()
    bcm_add_subdirectory(message_definition)
endif()
